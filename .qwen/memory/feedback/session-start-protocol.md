---
name: Follow session start protocol
description: User expects the session start protocol from AGENTS.md to be followed at the beginning of each session, including explicitly describing standards
type: feedback
---

Always execute the session start protocol from AGENTS.md at the beginning of a session: greet briefly, **describe** standards extracted from AGENTS.md (not just acknowledge them), display any recorded memories, then apply directives.

**Why:** The user explicitly called out "You don't describe standars ..." when I only acknowledged reading the standards without actually listing/describing them. Merely saying "I've acknowledged the standards" is insufficient — the user expects the standards to be explicitly described.

**How to apply:** At the start of every new session, infer and **describe** current standards from AGENTS.md by category (build/format, language, logging, control flow, etc.) rather than using hardcoded templates or a simple acknowledgment. Show existing memories if any exist.
