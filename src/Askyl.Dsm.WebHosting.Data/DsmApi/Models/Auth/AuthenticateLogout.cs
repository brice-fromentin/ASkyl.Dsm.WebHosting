namespace Askyl.Dsm.WebHosting.Data.DsmApi.Models.Auth;

/// <summary>
/// Empty entry for SYNO.API.Auth.logout — the SID travels in the request cookie, so no payload.
/// </summary>
public record AuthenticateLogout;
