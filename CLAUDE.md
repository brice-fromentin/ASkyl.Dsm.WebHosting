# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

`AGENTS.md` is the authoritative standards document; `AGENTS-WORKING-PREFERENCES.md` records how the
maintainer wants to be worked with. Both are imported below, so this file never restates them and cannot
drift from them. Add Claude-specific instructions here, under the imports — never a second copy of a rule
that already lives in `AGENTS.md`.

@AGENTS.md
@AGENTS-WORKING-PREFERENCES.md
